################# TASK ################# 
#
# 1. Replace both 'USERNAME' with your own name
# 2. Delete the not required '/static/' location
# 3. Replace everything within the '/app' location block with 'return 404;'
# 4. Change the 'max-age=31536000' to 'max-age=11536'
# 5. Select everything with the visual block in the '/app' location and indent it with '>' -> use <v>
# 6. Copy everything within the 'location /dashboard/' block and paste it below the 'location /app/' block
# 7. Search and replace the proxy pass port from 3000 to 5000 using the 'sed' command -> :%s/

################# ADVANCED TASKS ################# 
#
# 1. Change the server_name from 'example.com' to 'www.mywebsite.com' WITHOUT using the 'sed' command and with less than 
# 2. Change everything within the "" at 'max-age' to 'my-age=25' with ONE command
# 3. Delete everything within the server block { } with ONE command
# 4. Delete everything within the server block { } with ONE command
#
################# SHOWCASE ################# 
#
# Visual selection to change multiple lines at once
-----------------------------------------------------------------
# Nginx Configuration

# Set the user and group for Nginx to run as
user USERNAME;
worker_processes auto;

# Set the error log path
error_log /var/log/nginx/error.log;

# Set the events block
events {
    worker_connections 1024;
}

# Set the HTTP block
http {
    # Define the MIME types
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    # Set the server block for the main website
    server {
        listen 80; # Listen on port 80

        # Server name and alias
        server_name example.com www.example.com;

        # Set the root directory for the website
        root /var/www/html;

        # Access log settings
        access_log /var/log/nginx/access.log;
        error_log /var/log/nginx/error.log;

        # Index file settings
        index index.html;

        # Location block for static files
        location /static/ {
            alias /var/www/static/;
        }

        # Location block for proxying requests to an application server
        location /app/ {
            proxy_pass http://localhost:3000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Location block for proxying requests to an additional server
        location /dashboard/ {
            proxy_pass http://localhost:8000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
        # Security headers
        add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;
        add_header X-Content-Type-Options nosniff;
        add_header X-Frame-Options "SAMEORIGIN";
        add_header X-XSS-Protection "1; mode=block";

        # Include additional configurations
        include /etc/nginx/conf.d/*.conf;
    }
    user2 USERNAME;
    # Include additional server blocks if needed
}

